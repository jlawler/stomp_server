module StompServer
  module TopicParser
    
  end
  class  TopicSubscriptions
    def conns_by_topic(dest)
    end
    def add_subscription(dest,user); end #returns an array of loaded dests we might want
  end
end
